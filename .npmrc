# Ensure catalog references are resolved correctly
save-prefix=""
# Ensure workspace protocol is handled correctly during publish
link-workspace-packages=true
# Use exact versions when resolving catalog entries
save-exact=true 